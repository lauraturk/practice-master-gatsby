import React from 'react';

// name doesn't matter, but helpful in react debugger
// this doesn't show up in development (production only)
export default function FourOhFourPage() {
  return (
    <div>
      <p>Hey! That page doesn't exist</p>
    </div>
  );
}
